<Page x:Class="vokzal.AddEditCrewPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      Title="Добавление/Редактирование бригады">

    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="{Binding PageTitle}" FontSize="20" FontWeight="Bold" Margin="0,0,0,10"/>

        <GroupBox Grid.Row="1" Header="Информация о бригаде" Padding="10">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <TextBlock Grid.Row="0" Grid.Column="0" Text="ID бригады:" Margin="5" VerticalAlignment="Center"/>
                <TextBlock Grid.Row="0" Grid.Column="1" Text="{Binding CurrentCrew.CrewID}" Margin="5" VerticalAlignment="Center" 
                          FontWeight="Bold" Foreground="Blue"/>

                <TextBlock Grid.Row="1" Grid.Column="0" Text="Сотрудник:" Margin="5" VerticalAlignment="Center"/>
                <ComboBox Grid.Row="1" Grid.Column="1" Name="EmployeeComboBox" Margin="5" Height="25"
                         DisplayMemberPath="FullName" SelectedValuePath="EmployeeID"
                         SelectedValue="{Binding CurrentCrew.EmployeeID}"/>

                <TextBlock Grid.Row="2" Grid.Column="0" Text="Поезд:" Margin="5" VerticalAlignment="Center"/>
                <ComboBox Grid.Row="2" Grid.Column="1" Name="TrainComboBox" Margin="5" Height="25"
                         DisplayMemberPath="TrainNumber" SelectedValuePath="TrainID"
                         SelectedValue="{Binding CurrentCrew.TrainID}"/>
            </Grid>
        </GroupBox>

        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,10">
            <Button Name="SaveBtn" Content="Сохранить" Click="SaveBtn_Click" Width="100" Margin="5"/>
            <Button Name="CancelBtn" Content="Отмена" Click="CancelBtn_Click" Width="100" Margin="5"/>
        </StackPanel>
    </Grid>
</Page>